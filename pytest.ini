[pytest]
testpaths = tests
addopts = -v --tb=short
markers =
    real: marks tests as requiring real files and Whisper model (deselect with -m "not real")
    llama: marks tests as requiring a running Ollama instance (deselect with -m "not llama")
